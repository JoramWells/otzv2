FROM node:20-alpine

ARG NODE_ENV

ENV NODE_ENV ${NODE_ENV}

WORKDIR /usr/src/app

COPY package*.json ./

RUN yarn install --omit=dev

COPY . .

RUN yarn run build

EXPOSE 3000

# ENV CHOKIDAR_USEPOLLING=true

CMD ["yarn","start"]